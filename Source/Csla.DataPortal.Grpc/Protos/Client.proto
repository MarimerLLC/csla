syntax = "proto3";

package Csla.GrpcChannel.Client;

service GrpcPortal {
    rpc Invoke (RequestMessage) returns (ResponseMessage) {}
}

message RequestMessage {
    bytes body = 1;
    string operation = 2;
}

message ResponseMessage {
    bytes body = 1;
}